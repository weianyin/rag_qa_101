# Source documents

Drop the chosen document set here as plain `.txt` or `.md` files
(sub-directories are fine — ingestion walks the tree recursively).

This directory's contents are gitignored except for this file — the corpus
is a local input, not something to commit.

Document set: **TBD** — first unchecked box in [README.md](../../README.md).
