# Copy to .env and fill in. .env is gitignored.

# Required — used for generation and for the eval-harness LLM judge (Week 2)
ANTHROPIC_API_KEY=

# Optional overrides — defaults live in src/rag_qa/config.py
# RAG_CHUNK_SIZE=800
# RAG_CHUNK_OVERLAP=150
# RAG_TOP_K=4
# RAG_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# RAG_CLAUDE_MODEL=claude-sonnet-5
# RAG_CHROMA_PERSIST_DIR=./chroma_db
# RAG_COLLECTION_NAME=rag_qa_101
